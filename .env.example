DATABASE_URL=
JWT_SECRET=
# SESSION_SECRET can be used instead of JWT_SECRET when the host provides it.
SESSION_SECRET=
JWT_EXPIRES_IN=30d
# Set PGSSLMODE=disable only for a local PostgreSQL instance.
PGSSLMODE=
# Optional when the API is hosted separately from the frontend.
CLIENT_ORIGIN=