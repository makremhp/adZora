import { useMemo, useState } from "react";

function MiniIcon({ name, size = 17 }) {
  const props = { width: size, height: size, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 1.8, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": "true" };
  const paths = {
    plus: <><path d="M12 5v14M5 12h14" /></>, globe: <><circle cx="12" cy="12" r="9" /><path d="M3 12h18M12 3a14 14 0 0 1 0 18" /></>,
    code: <><path d="m8 9-4 3 4 3M16 9l4 3-4 3M14 5l-4 14" /></>, link: <><path d="M10 13a5 5 0 0 0 7.1.1l2-2a5 5 0 0 0-7.1-7.1l-1.1 1.1" /><path d="M14 11a5 5 0 0 0-7.1-.1l-2 2A5 5 0 0 0 7 20l1.1-1.1" /></>,
    copy: <><rect x="8" y="8" width="11" height="12" rx="2" /><path d="M16 8V6a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h2" /></>, check: <path d="m5 12 4 4L19 6" />, arrow: <path d="m9 18 6-6-6-6" />, wallet: <><path d="M4 6h15a2 2 0 0 1 2 2v11H4a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h14" /><path d="M16 13h5" /><circle cx="16" cy="13" r=".5" /></>, clock: <><circle cx="12" cy="12" r="9" /><path d="M12 7v5l3 2" /></>, trend: <><path d="M3 17 9 11l4 4 8-9" /><path d="M16 6h5v5" /></>,
  };
  return <svg {...props}>{paths[name] || paths.plus}</svg>;
}
function PageHeader({ title, description, action, onAction, icon = "plus" }) { return <div className="workspace-page-header"><div><span className="eyebrow">PUBLISHER / INVENTORY</span><h1>{title}</h1><p>{description}</p></div>{action && <button className="primary-button" type="button" onClick={onAction}><MiniIcon name={icon} size={17} />{action}</button>}</div>; }
function EmptyState({ icon = "globe", title, description, action, onAction }) { return <div className="empty-state"><span className="empty-state-icon"><MiniIcon name={icon} size={25} /></span><h2>{title}</h2><p>{description}</p>{action && <button className="secondary-button" type="button" onClick={onAction}><MiniIcon name="plus" size={16} />{action}</button>}</div>; }
function Field({ label, hint, error, children }) { return <label className="field"><span>{label}</span>{children}{hint && !error && <small>{hint}</small>}{error && <small className="field-error">{error}</small>}</label>; }
const blankWebsite = { name: "", url: "" };
function makeToken() { const alphabet = "ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789"; const random = typeof crypto !== "undefined" && crypto.getRandomValues ? crypto.getRandomValues(new Uint32Array(8)) : Array.from({ length: 8 }, () => Math.floor(Math.random() * 100000)); return Array.from(random, value => alphabet[value % alphabet.length]).join(""); }
function directLinkFor(website) { const token = makeToken(); return { token, url: "https://ad-zora.vercel.app/d/" + token, destination: website.url, clicks: 0, status: "Active", createdAt: new Date().toISOString() }; }
function WebsiteForm({ form, setForm, errors, onCancel, onSubmit }) { const update = (key, value) => setForm(previous => ({ ...previous, [key]: value })); return <section className="form-panel light-panel"><div className="panel-heading"><div><span className="eyebrow">PUBLISHER / WEBSITE</span><h2>Add website</h2></div><button className="link-button" type="button" onClick={onCancel}>Cancel</button></div><p className="form-intro">أدخل Website Name وWebsite URL فقط. لا يختار الناشر Banner أو Video أو Creative أو حجم إعلان.</p><div className="form-grid"><Field label="Website Name" error={errors.name}><input className="input" value={form.name} onChange={event => update("name", event.target.value)} placeholder="My website" /></Field><Field label="Website URL" hint="Use a full HTTPS address" error={errors.url}><input className="input" type="url" value={form.url} onChange={event => update("url", event.target.value)} placeholder="https://example.com" /></Field></div><div className="form-actions"><button className="primary-button" type="button" onClick={onSubmit}><MiniIcon name="check" size={16} />Add website</button><button className="ghost-button" type="button" onClick={onCancel}>Cancel</button></div></section>; }
function LinkBox({ website, onCopy, copiedId }) { if (!website.directLink) return <div className="direct-link-empty"><MiniIcon name="link" size={17} /><span><strong>No Direct Link yet</strong><small>Generate a unique link associated with this Publisher website.</small></span></div>; return <div className="direct-link-box"><div className="direct-link-label"><span><MiniIcon name="link" size={16} />Direct Link</span><span className="status-badge ready">Active</span></div><code>{website.directLink.url}</code><div className="direct-link-meta"><span>Destination: {website.directLink.destination}</span><span>{website.directLink.clicks || 0} clicks · Frontend demo</span></div><button className="ghost-button" type="button" onClick={() => onCopy(website)}><MiniIcon name={copiedId === website.id ? "check" : "copy"} size={16} />{copiedId === website.id ? "Copied" : "Copy Direct Link"}</button></div>; }
function WebsitesPage({ data, setData, onNavigate, setNotice }) {
  const [showForm, setShowForm] = useState(data.websites.length === 0); const [form, setForm] = useState(blankWebsite); const [errors, setErrors] = useState({}); const [copiedId, setCopiedId] = useState("");
  const addWebsite = () => { const next = {}; if (!form.name.trim()) next.name = "Enter a website name."; const normalized = form.url.trim().startsWith("http") ? form.url.trim() : "https://" + form.url.trim(); try { const parsed = new URL(normalized); if (parsed.protocol !== "https:" || !parsed.hostname.includes(".")) next.url = "Enter a valid HTTPS website address."; } catch { next.url = "Enter a valid HTTPS website address."; } setErrors(next); if (Object.keys(next).length) return; const website = { id: "website-" + Date.now(), name: form.name.trim(), url: new URL(normalized).toString(), status: "Pending", directLink: null, impressions: 0, clicks: 0 }; setData(previous => ({ ...previous, websites: [...previous.websites, website] })); setForm(blankWebsite); setErrors({}); setShowForm(false); setNotice("Website added. Generate a Direct Link or copy the Universal Code."); };
  const createLink = website => { setData(previous => ({ ...previous, websites: previous.websites.map(item => item.id === website.id ? { ...item, directLink: directLinkFor(website) } : item) })); setNotice("Direct Link created. The token is unique to this website in the demo."); };
  const copyLink = async website => { try { await navigator.clipboard.writeText(website.directLink.url); setCopiedId(website.id); setNotice("Direct Link copied."); setTimeout(() => setCopiedId(""), 1800); } catch { setNotice("Copy is unavailable in this browser. Select the link manually."); } };
  return <div className="workspace-page"><PageHeader title="My Websites" description="أضف Website Name وWebsite URL فقط. بعد ذلك اختر Universal Code أو أنشئ Direct Link خاصًا بالموقع." action="Add Website" onAction={() => setShowForm(true)} />{showForm && <WebsiteForm form={form} setForm={setForm} errors={errors} onCancel={() => { setShowForm(false); setErrors({}); }} onSubmit={addWebsite} />}{!data.websites.length && !showForm ? <section className="light-panel"><EmptyState title="No websites yet" description="أضف موقعك للحصول على Universal AdZora Code وإنشاء Direct Link." action="Add Website" onAction={() => setShowForm(true)} /></section> : data.websites.length > 0 && <section className="light-panel data-panel"><div className="panel-heading"><div><span className="eyebrow">PUBLISHER WEBSITES</span><h2>{data.websites.length} website{data.websites.length === 1 ? "" : "s"}</h2></div><span className="result-count">No ad format selection</span></div><div className="data-list">{data.websites.map(website => <div className="publisher-website-card" key={website.id}><div className="data-row publisher-website-row"><div className="row-main"><span className="row-icon"><MiniIcon name="globe" /></span><div><strong>{website.name}</strong><small>{website.url}</small></div></div><span className="status-badge pending">{website.status}</span></div><div className="publisher-website-actions"><button className="secondary-button" type="button" onClick={() => createLink(website)}><MiniIcon name="link" size={16} />{website.directLink ? "Regenerate Direct Link" : "Create Direct Link"}</button><button className="ghost-button" type="button" onClick={() => onNavigate("ad-codes")}><MiniIcon name="code" size={16} />Universal Code</button></div><LinkBox website={website} onCopy={copyLink} copiedId={copiedId} /></div>)}</div></section>}</div>; }
function AdCodesPage({ data, onNavigate, setNotice, setData }) {
  const [copiedId, setCopiedId] = useState(""); const codeFor = website => "<!-- AdZora Universal Code -->" + String.fromCharCode(10) + "<script src=\"https://ad-zora.vercel.app/ad.js\" data-adzora-website=\"" + website.id + "\" async></script>";
  const copyCode = async website => { try { await navigator.clipboard.writeText(codeFor(website)); setCopiedId(website.id); setNotice("Universal AdZora Code copied."); setTimeout(() => setCopiedId(""), 1800); } catch { setNotice("Copy is unavailable in this browser."); } };
  const createLink = website => { setData(previous => ({ ...previous, websites: previous.websites.map(item => item.id === website.id ? { ...item, directLink: directLinkFor(website) } : item) })); setNotice("Direct Link created for this website."); };
  return <div className="workspace-page"><PageHeader title="Universal AdZora Code" description="انسخ كودًا واحدًا إلى موقعك. الناشر لا يختار Campaign أو Creative أو Image أو Video أو Ad Size." action="Add Website" onAction={() => onNavigate("websites")} />{!data.websites.length ? <section className="light-panel"><EmptyState icon="code" title="Add a website first" description="بعد إضافة Website Name وWebsite URL سيظهر Universal AdZora Code هنا." action="Add Website" onAction={() => onNavigate("websites")} /></section> : <div className="code-list">{data.websites.map(website => <section className="light-panel code-card" key={website.id}><div className="code-card-heading"><div className="row-main"><span className="row-icon"><MiniIcon name="code" /></span><div><strong>{website.name}</strong><small>{website.url}</small></div></div><span className="status-badge ready">Universal</span></div><div className="code-note"><strong>One universal code for this website</strong><span>Ad Server will choose an eligible campaign and Creative later. Publisher settings do not include ad type, size, or Creative.</span></div><pre className="code-box"><code>{codeFor(website)}</code></pre><div className="code-actions"><button className="primary-button" type="button" onClick={() => copyCode(website)}><MiniIcon name={copiedId === website.id ? "check" : "copy"} size={16} />{copiedId === website.id ? "Copied" : "Copy Universal Code"}</button><button className="ghost-button" type="button" onClick={() => onNavigate("websites")}><MiniIcon name="globe" size={16} />Manage website</button></div>{website.directLink ? <LinkBox website={website} onCopy={async item => { try { await navigator.clipboard.writeText(item.directLink.url); setNotice("Direct Link copied."); } catch { setNotice("Copy is unavailable in this browser."); } }} copiedId="" /> : <button className="secondary-button direct-link-create" type="button" onClick={() => createLink(website)}><MiniIcon name="link" size={16} />Create Direct Link for this website</button>}<div className="frontend-only-note"><strong>Frontend-only boundary</strong><span>The /d/ endpoint, click tracking, destination validation, and server-side attribution require a backend and are not active in this UI build.</span></div></section>)}</div>}</div>; }
function FinanceMetrics({ finance }) { const metrics = [["Available Earnings", finance.available, "wallet"], ["Pending Earnings", finance.pending, "clock"], ["Total Earned", finance.earned, "trend"], ["Withdrawn", finance.withdrawn, "trend"]]; return <section className="metric-grid financial-metrics">{metrics.map(([label, value, icon]) => <article className="metric-card" key={label}><div className="metric-top"><span className="metric-icon"><MiniIcon name={icon} size={16} /></span><span className="metric-label">{label}</span></div><strong className="metric-value">{"$"}{value.toFixed(2)}</strong><span className="metric-hint">Frontend demo value</span></article>)}</section>; }
function EarningsPage({ finance, onNavigate }) { return <div className="workspace-page"><PageHeader title="Publisher Earnings" description="تابع أرباح الناشر منفصلة عن Advertiser Spend. هذه الأرقام تجريبية وليست محاسبة فعلية." action="Withdraw earnings" onAction={() => onNavigate("withdrawals")} icon="arrow" /><FinanceMetrics finance={finance} /><section className="light-panel"><div className="panel-heading"><div><span className="eyebrow">TRACKING ROADMAP</span><h2>Publisher revenue events</h2></div></div><div className="event-chip-list"><span>Impression</span><span>Click</span><span>Video View</span><span>Direct Link Click</span></div><p className="muted-copy">الأحداث جاهزة كنموذج بيانات، لكن لا تعتمد الأرباح أو الإحصائيات على localStorage ولا توجد خدمة Tracking متصلة بعد.</p></section></div>; }
function WithdrawalsPage({ finance, withdrawals, setFinance, setWithdrawals, setNotice }) { const [amount, setAmount] = useState(""); const [error, setError] = useState(""); const submit = () => { const value = Number(amount); if (!value || value < finance.minimum) { setError("The amount is below the minimum withdrawal."); return; } if (value > finance.available) { setError("The amount exceeds available earnings."); return; } const item = { id: "withdrawal-" + Date.now(), amount: value, status: "Pending", date: new Date().toLocaleDateString("en-US") }; setFinance(previous => ({ ...previous, available: previous.available - value })); setWithdrawals(previous => [...previous, item]); setAmount(""); setError(""); setNotice("Withdrawal saved as a demo Pending request. No money was moved."); }; return <div className="workspace-page"><PageHeader title="Withdrawals" description="طلب سحب منفصل عن إنفاق المعلن. لا يوجد Payment Provider متصل." /><section className="form-panel light-panel"><div className="withdraw-balance"><span>Available Earnings</span><strong>{"$"}{finance.available.toFixed(2)}</strong><small>Minimum: {"$"}{finance.minimum.toFixed(2)}</small></div><div className="form-grid"><Field label="Amount" error={error}><input className="input" type="number" value={amount} min={finance.minimum} onChange={event => { setAmount(event.target.value); setError(""); }} placeholder="50" /></Field><Field label="Payment method"><select className="input select"><option>Bank transfer (demo)</option><option>Manual review (demo)</option></select></Field></div><button className="primary-button" type="button" onClick={submit}><MiniIcon name="check" size={16} />Create demo request</button></section><section className="light-panel"><div className="panel-heading"><div><span className="eyebrow">WITHDRAWAL STATUS</span><h2>Recent requests</h2></div><span className="result-count">{withdrawals.length} records</span></div>{!withdrawals.length ? <EmptyState icon="trend" title="No withdrawals yet" description="سيظهر طلب السحب التجريبي هنا بحالة Pending." /> : <div className="data-list">{withdrawals.map(item => <div className="data-row compact-finance-row" key={item.id}><div className="row-main"><span className="row-icon"><MiniIcon name="trend" /></span><div><strong>{"$"}{item.amount.toFixed(2)}</strong><small>{item.date}</small></div></div><span className="status-badge pending">{item.status}</span></div>)}</div>}</section></div>; }
function PublisherTransactions({ withdrawals }) { return <div className="workspace-page"><PageHeader title="Publisher Transactions" description="سجل منفصل لأرباح الناشر والتعديلات والسحوبات، دون خلطه بإنفاق المعلن." /><section className="light-panel data-panel"><div className="panel-heading"><div><span className="eyebrow">TRANSACTION HISTORY</span><h2>{withdrawals.length} records</h2></div></div>{!withdrawals.length ? <EmptyState icon="code" title="No transactions yet" description="ستظهر Earnings وAdjustments وWithdrawals هنا عند وجود بيانات." /> : <div className="data-list">{withdrawals.map(item => <div className="data-row" key={item.id}><div className="row-main"><span className="row-icon"><MiniIcon name="trend" /></span><div><strong>Withdrawal · {"$"}{item.amount.toFixed(2)}</strong><small>{item.date}</small></div></div><span className="status-badge pending">{item.status}</span></div>)}</div>}</section></div>; }
export default function PublisherWorkspace({ page, data, setData, onNavigate, finance = { available: 125, pending: 32.5, earned: 642.5, withdrawn: 485, minimum: 50 }, setFinance = () => {}, withdrawals = [], setWithdrawals = () => {} }) { const [notice, setNotice] = useState(""); const content = useMemo(() => page, [page]); return <div className="publisher-workspace">{notice && <div className="notice" role="status"><span>{notice}</span><button type="button" onClick={() => setNotice("")}>×</button></div>}{content === "websites" && <WebsitesPage data={data} setData={setData} onNavigate={onNavigate} setNotice={setNotice} />}{content === "ad-codes" && <AdCodesPage data={data} setData={setData} onNavigate={onNavigate} setNotice={setNotice} />}{content === "earnings" && <EarningsPage finance={finance} onNavigate={onNavigate} />}{content === "withdrawals" && <WithdrawalsPage finance={finance} withdrawals={withdrawals} setFinance={setFinance} setWithdrawals={setWithdrawals} setNotice={setNotice} />}{content === "transactions" && <PublisherTransactions withdrawals={withdrawals} />}</div>; }
